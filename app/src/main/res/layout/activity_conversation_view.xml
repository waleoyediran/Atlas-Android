<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:atlas="http://schemas.android.com/apk/res-auto"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".MainActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/conversation_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_above="@+id/sendbox"
        android:layout_alignParentTop="true"
        />

    <com.layer.atlas.view.SendBox
        android:id="@+id/sendbox"
        android:layout_width="match_parent"
        android:layout_height="@dimen/atlas_sendbox_height"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        atlas:photoDrawable="@mipmap/ic_launcher"
        atlas:sendDrawable="@mipmap/ic_launcher"
        />

</RelativeLayout>
